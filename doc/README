Setup the database:

add user with pass according to mysql.ini
create user 'aqctrl'@'localhost' IDENTIFIED BY 'aqctrl_pass';
grant all privileges on *.* to 'aqctrl'@'localhost' with grant option;
flush privileges;

Setup with Apache:

Make sure you have php enabled & working
Make sure you have your Mysql server working

Security:

- For each arduino: make sure you give it a unique ID and key; don't share the key with others
- Make sure to change the Mysql password for the aqctrl user
- Add your own wireless info for the arduino
- Check and make sure that debug mode is off

